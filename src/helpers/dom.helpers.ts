// // Función para agregar o quitar la clase 'draggable' al body
// function toggleDraggable(isDraggable: boolean) {
//     if (isDraggable) {
//         document.body.classList.add('draggable');
//         return;
//     }
//     document.body.classList.remove('draggable');
// }

// // Escuchar el evento 'keydown' para detectar cuando se presiona la tecla Ctrl
// document.addEventListener('keydown', (event) => {
//     if (event.ctrlKey) {
//         toggleDraggable(true);
//     }
// });

// // Escuchar el evento 'keyup' para detectar cuando se suelta la tecla Ctrl
// document.addEventListener('keyup', (event) => {
//     if (!event.ctrlKey) {
//         toggleDraggable(false);
//     }
// });
